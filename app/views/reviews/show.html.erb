
<div class="container p-3 bg-light">
  <h2> Reviews </h2>
  <% @restaurant.reviews.each do |review| %>
    <div class="card m-4 p-3">
      <h3><%= @restaurant.name %></h3>
      <p><%= review.content %></p>
      <p><%= '⭐️' * review.rating %></p>
      <% end %>
    </div>
 </div>

  <%# <div class="btn btn-outline-primary">
    <%= link_to "Add a new review", new_restaurant_review_path %>
  <%# </div>
</div> %>
